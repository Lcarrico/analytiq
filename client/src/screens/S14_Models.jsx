// retired R33S1E1-US1 (2026-07-06) — the models pillar owns /app/models now
// (ch18 frames): overview (E1), run detail (E2), model card (E3), leaderboard
// + feature manifest + retrain center (E4). S14's quick actions (retrain,
// evaluate challenger, archive) rehomed onto the overview rows; the modeler
// dry-run + custom-feature composer rehome with the E4 feature manifest.
// File removal is blocked on this mount (adaptation ledger) — tombstoned.
export default function RetiredS14() { return null; }
