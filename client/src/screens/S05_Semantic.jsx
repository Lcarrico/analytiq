// retired R32S2E1-US1 (2026-07-05) — the semantic layer overview / explores /
// detail screens own /app/semantic now (ch17 frames). Review actions live in
// the governance review queue (R32S1E2).
// File removal is blocked on this mount (adaptation ledger) — tombstoned.
export default function RetiredS05() { return null; }
