// retired R30S3E7-US1 (2026-07-05) — the Create Workbench owns the whole
// ask→plan→build→canvas loop (R30S2); /app/create named children redirect.
// File removal is blocked on this mount (adaptation ledger) — tombstoned.
export default function RetiredS06() { return null; }
